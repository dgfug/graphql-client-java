# GraphQL Server API:
# https://api.spacex.land/graphql/

extend type Query {
    historiesBasic: [Histories]
    usersBasic: [User]
}

type Histories {
    title: String
    details: String
}

type User {
    id: String
    name: String
    rocket: String
    timestamp: String
    twitter: String
}