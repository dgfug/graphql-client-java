# GraphQL Server API:
# https://api.spacex.land/graphql/

extend type Query {
    historiesBasic: [Histories]
    usersBasic: [MyUser]
}

type Histories {
    title: String
    details: String
}

type MyUser {
    id: String
    name: String
    rocket: String
    timestamp: String
    twitter: String
}